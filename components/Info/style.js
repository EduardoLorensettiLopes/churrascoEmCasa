import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        marginTop: 15,
        alignItems: "center",
    },
    cardTitle:{
        width: 350,
        textAlign: "center",
        fontSize: 20,
        fontWeight: "bold",
    },
    cardDescription: {
        marginTop: 20,
        lineHeight: 20,
        width: 360,
        textAlign: "center"
    }
})

export default styles;